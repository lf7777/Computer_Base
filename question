1.对字节流的理解,Unicode中文通常两个字节,中文二字为每个字为3字节,一共六个字节,为啥可以转为bytes,bytes占一个字节啊.
答:这是个弱智问题,bytes就是占一个字节,进行存储或者传输的时候,bytes就会变为6字节.
数据传输不能传输Unicode,只能传字节流,即每一个字节一个个的传,

2.为什么没有A B分区?

答 : 因为A B分区在早期的时候被3.5英寸和2.5英寸软盘占用.虽然软盘被科技所淘汰了,但是磁盘顺序依然保持不变.
